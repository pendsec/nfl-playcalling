# V2 — "Right Tools, Simple Problem".
# Install binaries only on the py3.13 / sklearn 1.8 / pandas 3 stack:
#   pip install --only-binary=:all: -r requirements.txt
nfl_data_py>=0.3.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pyarrow>=12.0.0
pyyaml>=6.0
pytest>=7.0
# V2 causal / modeling tooling
lightgbm>=4.0
dowhy>=0.11
networkx>=3.0
matplotlib>=3.7
# econml (causal-forest CATE) is deferred to V3 — intentionally not pinned here.
