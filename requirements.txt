# V1 — Walking Skeleton dependencies (deliberately minimal).
# Heavier causal tooling (econml, dowhy, lightgbm) arrives in V2+.
nfl_data_py>=0.3.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pyarrow>=12.0.0
pyyaml>=6.0
pytest>=7.0
